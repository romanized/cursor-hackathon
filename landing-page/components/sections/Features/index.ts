/**
 * Public surface of the Features section. `app/page.tsx` imports the island from
 * here; everything else (data, icons, animation) stays section-private.
 */
export { Features } from "./Features";
