/**
 * Barrel for the RenderPin section — the signature pinned transformation.
 *
 * Exposes the section island only; the animation factory stays internal so the
 * scene is reshaped through one entry point.
 */
export { RenderPin, type RenderPinProps } from "./RenderPin";
