/**
 * Barrel for the SneakPeek section — the peek-from-bottom studio panel demo.
 *
 * Exposes the section island only; the animation factory + static helper stay
 * internal so the scene is reshaped through one entry point.
 */
export { SneakPeek, type SneakPeekProps } from "./SneakPeek";
