/**
 * Footer section barrel.
 *
 * Page composition imports the island from here so `app/page.tsx` reads against
 * section names, not file paths. The animation factory stays internal.
 */
export { Footer } from "./Footer";
